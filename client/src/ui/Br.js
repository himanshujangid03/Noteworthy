import styled from "styled-components";

const Br = styled.div`
  height: 2px;
  background: var(--border-color);
  width: 60rem;
  max-width: 90%;
  margin: 1.5rem auto;
  transition: all 0.2s;
`;
export default Br;
